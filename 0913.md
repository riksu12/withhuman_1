# 홈 데스크톱 원격 접속 자동 구성 매뉴얼 v2 (Tailscale + WinVNC4)

**이 문서는 이전 버전("WinVNC4 + 공유기 포트포워딩")을 대체한다.** 이유: 공유기 포트포워딩 방식은 공인 IP 변경, 공유기 UPnP 지원 여부, DDNS 등 관리 포인트가 계속 남아있어 "한 번 설정하면 손 안 대도 됨"이라는 요구와 맞지 않는다. Tailscale(사설 메시 VPN)을 쓰면 포트포워딩·DDNS·공유기 설정이 전부 필요 없어지고, 데스크톱과 노트북이 어떤 네트워크에 있든 항상 자동으로 서로를 찾아 연결된다. VNC 자체는 인터넷에 노출되지 않고 이 사설망 안에서만 열린다.

**실행 대상**: Claude Code가 집 데스크톱 PC에 디바이스 접근 권한으로 직접 접속해 아래 명령을 순서대로, 확인 절차 없이 끝까지 실행한다. 모호하거나 이 문서로 판단이 안 되는 상황에서만 사용자에게 보고하고, 그 외에는 여기 적힌 기본값/자동 판단 로직대로 진행한다.

---

## 이 문서 전체에서 사람이 직접 해야 하는 유일한 준비물

아래 한 가지만 사람이 미리 해두면, 나머지는 전부 자동 실행된다.

1. https://tailscale.com 접속 → 기존 Google/Microsoft/GitHub 계정 등으로 무료 가입 (약 30초, 개인 사용은 무료 플랜으로 충분).
2. 관리자 콘솔(https://login.tailscale.com/admin/settings/keys) 접속 → **Generate auth key** 클릭.
   - `Reusable` 켜기 (여러 번/여러 기기에서 같은 키로 등록 가능하게).
   - `Expiration`은 가능하면 늘리거나(최대치), 나중에 등록된 기기 자체에서 "Disable key expiry"로 재고정할 것(아래 4단계에서 안내).
   - 생성된 키(`tskey-...`로 시작)를 복사.
3. 아래 `TAILSCALE_AUTHKEY` 자리에 그 키를 붙여넣는다. **이 문서에서 사람이 입력하는 값은 이것 하나뿐이다.**

```
TAILSCALE_AUTHKEY = "tskey-여기에_붙여넣기"
```

> 이 키는 "우리 집 데스크톱을 내 Tailscale 계정에 등록해도 좋다"는 1회성 자격증명이다. VNC 비밀번호가 아니며, 노출되어도 계정 침해 없이는 원격 데스크톱 제어에 직접 쓰이지 않는다. 그래도 이 md 파일을 아무 데나 공유하지 말 것.

---

## 0. 자동 상태 저장 (재실행 시 값 재입력 불필요)

Claude Code는 아래 상태 파일을 확인하여, 이미 완료된 단계는 건너뛰고 필요한 값(예: 데스크톱의 Tailscale IP, VNC 포트, 비밀번호 파일 위치)은 새로 계산하지 않고 이 파일에서 읽는다. **이 문서를 다시 실행해도 사람이 값을 다시 채워 넣을 필요가 없는 이유가 이것이다.**

```powershell
$StateDir  = "C:\RemoteAccessSetup"
$StatePath = "$StateDir\state.json"
New-Item -ItemType Directory -Path $StateDir -Force | Out-Null

if (Test-Path $StatePath) {
    $State = Get-Content $StatePath -Raw | ConvertFrom-Json
} else {
    $State = [PSCustomObject]@{
        VncPort         = 5900
        VncPasswordFile = "$StateDir\vnc_password.secure.txt"
        TailscaleReady  = $false
        VncReady        = $false
        FirewallReady   = $false
        DesktopHostname = $env:COMPUTERNAME.ToLower() + "-home"
    }
    $State | ConvertTo-Json | Set-Content $StatePath
}
```

이후 모든 단계는 이 `$State` 객체와 `$StateDir`을 기준으로 동작하며, 끝에서 `$State`를 다시 저장한다. 재실행 시에는 이미 끝난 단계(`...Ready = $true`)를 건너뛰고 남은 것만 처리한다.

---

## 1. Tailscale 설치 및 연결 (완전 자동)

```powershell
# 1-1. 설치 여부 확인 후 없으면 설치 (winget 사용, 없으면 공식 MSI로 폴백)
if (-not (Get-Command tailscale -ErrorAction SilentlyContinue)) {
    try {
        winget install --id Tailscale.Tailscale -e --silent --accept-source-agreements --accept-package-agreements
    } catch {
        $msi = "$env:TEMP\tailscale-setup.msi"
        Invoke-WebRequest -Uri "https://pkgs.tailscale.com/stable/tailscale-setup-latest.msi" -OutFile $msi
        Start-Process msiexec.exe -ArgumentList "/i `"$msi`" /quiet /norestart" -Wait
    }
}

# 1-2. 서비스 자동시작 확인
Set-Service -Name "Tailscale" -StartupType Automatic -ErrorAction SilentlyContinue
Start-Service -Name "Tailscale" -ErrorAction SilentlyContinue

# 1-3. 인증키로 비대화형 연결 (이미 연결돼 있으면 그대로 유지)
$tsStatus = & tailscale status --json 2>$null | ConvertFrom-Json
if (-not $tsStatus -or $tsStatus.BackendState -ne "Running") {
    & tailscale up --authkey="$TAILSCALE_AUTHKEY" --hostname="$($State.DesktopHostname)" --accept-routes
}

# 1-4. 이 기기에 할당된 Tailscale IP 확보
Start-Sleep -Seconds 3
$TsIp = (& tailscale ip -4).Trim()
$State.DesktopTailscaleIp = $TsIp
$State.TailscaleReady = $true
$State | ConvertTo-Json | Set-Content $StatePath
```

- `tailscale ip -4`로 얻은 주소(예: `100.x.x.x`)가 앞으로 노트북에서 이 데스크톱에 접속할 고정 주소다. 공인 IP나 공유기 설정과 무관하게 항상 이 주소로 접속 가능하다.
- **한 번만 사람이 확인할 것 (선택이지만 강력 권장)**: Tailscale 관리 콘솔(https://login.tailscale.com/admin/machines) 에서 방금 등록된 기기를 찾아 `...` 메뉴 → **Disable key expiry** 클릭. 이걸 해두면 이후 몇 달이 지나도 재인증 없이 계속 연결이 유지된다. 안 해두면 기본 설정상 일정 기간 후 재인증이 필요할 수 있다.

---

## 2. WinVNC4 설치 (없을 때만)

```powershell
$vncInstalled = Get-Service | Where-Object { $_.DisplayName -match 'VNC' }
if (-not $vncInstalled) {
    # 설치파일은 사용자가 realvnc.com 공식 아카이브 등 신뢰 가능한 출처에서 미리 받아둔 것을 사용한다.
    # 아래 경로에 파일이 없으면 이 단계에서 멈추고 사용자에게 설치파일 위치를 보고한다 (자동 다운로드하지 않음 — 출처 불명 바이너리 설치 방지).
    $VncInstaller = "C:\Users\$env:USERNAME\Downloads\VNC-Free-4.1.3-x86.exe"
    if (Test-Path $VncInstaller) {
        Start-Process -FilePath $VncInstaller -Wait
    } else {
        Write-Warning "VNC 설치파일을 찾을 수 없음: $VncInstaller — 사용자에게 위치 확인 요청 필요"
    }
}
```

설치 후 실제 지원 명령줄 옵션을 한 번 확인하고(빌드마다 다를 수 있음), 서비스 등록·자동시작을 맞춘다.

```powershell
$VncDir = (Get-ChildItem "C:\Program Files*\RealVNC*" -Directory -ErrorAction SilentlyContinue | Select-Object -First 1).FullName
& "$VncDir\WinVNC4.exe" -help 2>$null | Out-File "$StateDir\winvnc_help.txt"

$svc = Get-Service | Where-Object { $_.DisplayName -match 'VNC' } | Select-Object -First 1
if ($svc) {
    Set-Service -Name $svc.Name -StartupType Automatic
    Start-Service -Name $svc.Name -ErrorAction SilentlyContinue
}
```

---

## 3. VNC 비밀번호 자동 생성 (사람에게 입력받지 않음)

사람에게 비밀번호를 물어보지 않는다는 요구에 맞춰, 강력한 비밀번호를 자동 생성하고 데스크톱의 현재 로그인 계정만 읽을 수 있는 파일에 저장한 뒤 1회 화면에 표시한다. **이 값은 이 md 파일이나 대화 기록에 평문으로 남기지 않는다.**

```powershell
if (-not (Test-Path $State.VncPasswordFile)) {
    Add-Type -AssemblyName System.Web
    $NewPassword = [System.Web.Security.Membership]::GeneratePassword(16, 4)
    Set-Content -Path $State.VncPasswordFile -Value $NewPassword
    icacls $State.VncPasswordFile /inheritance:r /grant:r "$($env:USERNAME):(R)" | Out-Null
    Write-Host "생성된 VNC 비밀번호 (지금 한 번만 표시됨, 비밀번호 관리자에 저장하세요): $NewPassword"
} else {
    $NewPassword = Get-Content $State.VncPasswordFile
}
```

이후 `vncpasswd.exe -service` (또는 2절에서 확인한 실제 지원 방식)로 이 값을 서비스 계정 비밀번호로 등록한다. 이 도구가 완전 비대화형 입력을 지원하지 않는 빌드라면, Claude Code는 화면 조작 권한이 있는 경우 설정창에 자동 입력하고, 없는 경우 이 단계만 "비밀번호가 `%StateDir%\vnc_password.secure.txt`에 준비되어 있으니 VNC 설정창에 붙여넣어 달라"고 사용자에게 1회 보고한다(이 문서에서 사람 개입이 남는 유일한 지점이며, 정말 예외적인 경우에만 발생).

---

## 4. 방화벽 — Tailscale 사설망에서만 허용, 공인 인터넷은 차단

```powershell
$TailscaleCgnatRange = "100.64.0.0/10"

if (-not (Get-NetFirewallRule -DisplayName "VNC over Tailscale" -ErrorAction SilentlyContinue)) {
    New-NetFirewallRule -DisplayName "VNC over Tailscale" `
      -Direction Inbound -Protocol TCP -LocalPort $State.VncPort `
      -RemoteAddress $TailscaleCgnatRange -Action Allow
}

# 공인 인터넷 대상 인바운드 규칙이 실수로 남아있지 않은지 확인 (있으면 제거 권장 — 지금은 포트포워딩 방식을 안 쓰므로)
Get-NetFirewallRule -DisplayName "VNC Server*" -ErrorAction SilentlyContinue |
  Where-Object { $_.DisplayName -notmatch "Tailscale" } |
  ForEach-Object { Write-Warning "이전 방식의 방화벽 규칙 발견: $($_.DisplayName) — 공유기 포트포워딩을 안 쓰기로 했다면 이 규칙은 제거를 권장" }

$State.FirewallReady = $true
$State | ConvertTo-Json | Set-Content $StatePath
```

이 규칙 덕분에 VNC 포트는 **Tailscale 사설망(100.64.0.0/10) 안에서 오는 연결만 허용**하고 일반 인터넷에서는 접근 자체가 불가능하다. 공유기 포트포워딩, DDNS는 아예 필요 없다.

---

## 5. 노트북 쪽 설정 (최초 1회, 기기당 한 번만)

Claude Code가 이 세션에서 노트북에도 디바이스 접근 권한이 있다면 아래를 동일하게 자동 실행한다(설치 → `tailscale up --authkey=... --accept-routes`). 그렇지 않다면 사람이 노트북에서 아래만 하면 된다(약 1분, 평생 한 번):

1. https://tailscale.com/download 에서 노트북 OS용 Tailscale 설치.
2. 실행 후 데스크톱과 **같은 계정**으로 로그인(가입 때 쓴 Google/Microsoft/GitHub 계정).
3. 완료. 이후로는 노트북이 어느 와이파이/테더링에 있든 자동으로 데스크톱과 사설망으로 연결된다.

노트북에서 VNC 뷰어(예: RealVNC Viewer, TightVNC Viewer 등 아무 표준 VNC 클라이언트)로 접속할 주소:

```
<데스크톱 Tailscale IP>:5900
```

또는 Tailscale 관리 콘솔에서 **MagicDNS**를 켜두면(설정 콘솔에서 토글 1번, 1회성) IP 대신 고정된 이름으로 접속 가능:

```
<DesktopHostname>.<tailnet-이름>.ts.net:5900
```

`<DesktopHostname>`은 0절 상태 파일의 `DesktopHostname` 값(기본: `<컴퓨터이름>-home`)이다.

---

## 6. 검증 (Claude Code가 자동 실행 후 스스로 확인)

```powershell
& tailscale status
Get-Service -Name "Tailscale"
Get-Service | Where-Object { $_.DisplayName -match 'VNC' }
Get-NetFirewallRule -DisplayName "VNC over Tailscale"
Test-NetConnection -ComputerName $State.DesktopTailscaleIp -Port $State.VncPort
```

위 결과가 모두 정상이면 (Tailscale 서비스 실행 중, VNC 서비스 실행 중, 방화벽 규칙 존재, 포트 응답) 설정이 끝난 것이다. 노트북에서 실제 접속 테스트는 사람이 1회 확인하는 것을 권장한다.

---

## 7. 이 문서의 자동화가 절대 하지 않는 것 (권한을 넓게 줘도 지키는 최소 안전장치)

"권한을 웬만큼 다 주겠다"는 요청을 받아들이되, 아래는 명시적 지시 없이는 자동 실행하지 않는다.

- Windows 방화벽 자체를 끄지 않는다(스코프가 좁은 허용 규칙만 추가).
- Windows Defender/UAC 등 OS 보안 기능을 비활성화하지 않는다.
- VNC 포트를 공인 인터넷(WAN)에 노출하는 방화벽 규칙이나 공유기 포트포워딩을 추가하지 않는다.
- 출처가 확인되지 않는 실행파일을 인터넷에서 임의로 받아 설치하지 않는다(Tailscale은 공식 배포 채널만 사용, VNC 설치파일은 사용자가 직접 받아둔 것만 사용).
- 생성된 VNC 비밀번호나 Tailscale authkey를 로그/문서/채팅 기록에 평문으로 남기지 않는다.

이 다섯 가지에 해당하는 상황이 오면, Claude Code는 그 지점에서만 사람에게 짧게 보고하고 나머지는 계속 자동 진행한다.

---

## 8. 재실행 시나리오 (예: PC 재설치, 하드웨어 교체)

이 문서를 그대로 다시 던지면 된다. `state.json`이 없으면 처음부터, 있으면 이미 끝난 단계는 건너뛰고 새 환경에 맞는 값만 다시 계산한다. Tailscale authkey가 만료/폐기되어 재인증이 실패하는 경우에만, 맨 위 "유일한 준비물" 단계를 반복해 새 키를 받아 상단 자리에 채워 넣으면 된다 — 그 외에는 사람이 손댈 부분이 없다.

---

## 변경 이력

| 날짜 | 변경 내용 |
|---|---|
| 2026-09-13 | v1: WinVNC4 + 공유기 포트포워딩 직접 노출 방식 |
| 2026-09-13 | v2(현재): Tailscale 사설망 기반으로 전면 재설계 — 포트포워딩/DDNS 제거, 완전 자동 재실행 가능하도록 상태 파일 도입, 비밀번호 자동 생성으로 변경 |
